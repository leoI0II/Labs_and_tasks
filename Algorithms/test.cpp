#include<iostream>
#include<string>
#include<fstream>
#include<filesystem>


int main(){

    // std::string a = "Hello World!";

    // std::cout << a << std::endl;
    // for (const auto& _char: a)
    //     std::cout << _char << " ";
    // std::cout << std::endl;
    // while (!a.empty()){
    //     std::cout << *(a.end()-1);
    //     // a.pop_back();
    //     a.erase(a.end()-1);
    // }

    // std::string d = "David", m = "Maggie", mn = "Manning";
    // std::cout << std::boolalpha << (mn > m) << std::endl;

    // __int128_t a = 0;
    // std::cout << sizeof(a) << std::endl;

    // std::ofstream a("some_file.txt");
    // if (a.is_open()) std::cout << "Eok" << std::endl;
    
    //WORK, but vscode gives errors
    // std::filesystem::path a = std::filesystem::current_path();
    // std::cout << a << std::endl;

    return 0;
}